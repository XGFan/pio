# Build artifacts
/piad
/cmd/piad/piad
/build/
/dist/
*.test
*.out
coverage.txt

# Swift / SwiftPM build cache (per-user, gigabytes)
ui/PIA/.build/
ui/PIA/Package.resolved

# Local data dirs (in case tests or dev runs drop them in-tree)
/data/
/tmp/
*.db
*.db-wal
*.db-shm
master.key
master.salt
api.port

# OMC and tooling state (per-user, not source)
.omc/state/
.omc/sessions/
.omc/project-memory.json
.omc/notepad.md
.omc/webshare-web-password.txt
ui/PIA/.omc/
.playwright-cli/

# Per-user MCP config (may contain API keys)
.mcp.json

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp
